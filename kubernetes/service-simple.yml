apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: default
  labels:
    env: production
    version: "1.0"
spec:
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
